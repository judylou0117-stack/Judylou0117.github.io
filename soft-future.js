(()=>{const home=document.querySelector('#home'),stage=document.querySelector('.portrait-stage'),fine=matchMedia('(hover:hover) and (pointer:fine)');let frame=0;const reset=()=>{cancelAnimationFrame(frame);home.style.setProperty('--px','0px');home.style.setProperty('--py','0px');document.querySelectorAll('.button.primary').forEach(b=>b.style.translate='0px 0px')};stage.addEventListener('pointermove',e=>{if(isReduced()||!fine.matches)return;const r=stage.getBoundingClientRect();const x=((e.clientX-r.left)/r.width-.5)*9,y=((e.clientY-r.top)/r.height-.5)*7;cancelAnimationFrame(frame);frame=requestAnimationFrame(()=>{home.style.setProperty('--px',x+'px');home.style.setProperty('--py',y+'px')})});stage.addEventListener('pointerleave',reset);document.querySelectorAll('.button.primary').forEach(b=>{b.addEventListener('pointermove',e=>{if(isReduced()||!fine.matches)return;const r=b.getBoundingClientRect();b.style.translate=`${Math.max(-3,Math.min(3,(e.clientX-r.left-r.width/2)*.04))}px ${Math.max(-2,Math.min(2,(e.clientY-r.top-r.height/2)*.06))}px`});b.addEventListener('pointerleave',()=>b.style.translate='0px 0px')});reduce.addEventListener('change',reset);fine.addEventListener('change',reset);document.querySelector('#motion-toggle').addEventListener('click',reset);document.addEventListener('visibilitychange',reset)})();
